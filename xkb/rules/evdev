// BEGIN Dustin's layout
! option    =   types
  custom:numpad_arrows = +custom(numpad_arrows)
  
! option    =   symbols
  custom:swap_ralt_rctl_rshift = +custom(swap_ralt_rctl_rshift)
  custom:numpad_arrow_tri = +custom(numpad_arrow_tri)
// END Dustin's layout
